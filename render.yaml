services:
  - type: web
    name: resume-analyzer-frontend
    runtime: static
    plan: free
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
